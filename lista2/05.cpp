#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, inp;
  cin >> n >> m;
  vector<int> tasks(m);
  
  for (int i = 0; i < m; i++) {
    cin >> inp;
    tasks.push_back(inp);
  }
}